/**
 * TODO:
 *   1. Extract the JWT from the HTTP Header: Authorization
 *   2. Verify if the token is valid ( see: jsonwebtoken package )
 *   3. Verify if user exist in Database
 *   4. Inject user into request.user for the next handlers
 */
